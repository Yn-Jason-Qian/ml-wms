# 复制为 .env.local 后按需修改（.env.local 已在 .gitignore 中，不会进仓库）

# 开发期后端地址：/api 与 /ws-stomp 的代理目标，默认 http://localhost:8080
VITE_DEV_PROXY_TARGET=

# WebSocket 地址。留空时按同源推导 ws(s)://<当前域名>/ws-stomp
# （开发期走 vite 代理，生产期走 nginx 反代）
VITE_WS_BASE_URL=
