# 部署脚本与容器配置在 Linux 容器 / 服务器上执行或读取，必须保持 LF 换行。
# 仓库在 Windows 上开发（core.autocrlf=true），不加约束时工作区会被转成 CRLF，
# 带 \r 的 shell 脚本在 Linux 上会直接报 "/bin/sh^M: bad interpreter"。
*.sh         text eol=lf
Dockerfile*  text eol=lf
*.conf       text eol=lf
deploy/**    text eol=lf
