<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.wms</groupId>
    <artifactId>wms-server</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>WMS Server</name>
    <description>Warehouse Management System - Parent POM</description>

    <modules>
        <module>wms-common</module>
        <module>wms-masterdata</module>
        <module>wms-inbound</module>
        <module>wms-outbound</module>
        <module>wms-inventory</module>
        <module>wms-strategy</module>
        <module>wms-task</module>
        <module>wms-print</module>
        <module>wms-web</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Framework versions -->
        <spring-boot.version>3.5.16</spring-boot.version>
        <mybatis-plus.version>3.5.9</mybatis-plus.version>
        <mysql-connector.version>8.4.0</mysql-connector.version>

        <!-- Utility versions -->
        <lombok.version>1.18.38</lombok.version>
        <hutool.version>5.8.35</hutool.version>
        <knife4j.version>4.5.0</knife4j.version>
        <!-- knife4j 4.5.0 传递依赖 springdoc 2.3.0，它调用 Spring Framework 6.2 中已被移除的
             ControllerAdviceBean(Object) 构造器（/v3/api-docs 会抛 NoSuchMethodError）。
             Spring Boot 3.5 对应 Spring Framework 6.2，因此把 springdoc 显式提升到 2.8.x。 -->
        <springdoc.version>2.8.6</springdoc.version>
        <mapstruct.version>1.6.3</mapstruct.version>
        <!-- 见 dependencyManagement 中 EasyExcel/POI 处说明：以下三项不在 Spring Boot BOM 管理范围内 -->
        <poi.version>5.4.1</poi.version>
        <commons-compress.version>1.26.2</commons-compress.version>
        <commons-lang3.version>3.18.0</commons-lang3.version>

        <!--
          以下三项用于覆盖 Spring Boot BOM 管理的版本，跟进它尚未合并的上游补丁
          （本项目是 import BOM 而非继承 spring-boot-starter-parent，因此无法用同名属性覆盖，
            见 dependencyManagement 中的处理）：
            tomcat  10.1.55       -> 10.1.59        修复 GHSA-9xv2-5v5q-p794 等（上游跳过了 10.1.58）
            jackson 2.21.4        -> 2.21.5         修复 GHSA-5gvw-p9qm-jgwh（@JsonView 绕过）等
            netty   4.1.135.Final -> 4.1.136.Final  修复 GHSA-558v-64gr-wgg4（Bzip2 解码死循环）
          均为同一次要版本线内的补丁升级，目标版本已用 OSV 核对无已知漏洞。
          Spring Boot 后续补丁版跟上后，这里即可删除。
        -->
        <tomcat.version>10.1.59</tomcat.version>
        <jackson.version>2.21.5</jackson.version>
        <netty.version>4.1.136.Final</netty.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!--
              覆盖 Spring Boot BOM 的版本：这两个 BOM 必须放在 spring-boot-dependencies 之前
              （Maven 对同一 artifact 采用先声明者优先），才能盖住 Spring Boot 管理的 jackson / netty 版本。
            -->
            <dependency>
                <groupId>com.fasterxml.jackson</groupId>
                <artifactId>jackson-bom</artifactId>
                <version>${jackson.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-bom</artifactId>
                <version>${netty.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- MyBatis-Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- MySQL -->
            <dependency>
                <groupId>com.mysql</groupId>
                <artifactId>mysql-connector-j</artifactId>
                <version>${mysql-connector.version}</version>
            </dependency>

            <!-- Internal modules -->
            <dependency>
                <groupId>com.wms</groupId>
                <artifactId>wms-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.wms</groupId>
                <artifactId>wms-masterdata</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.wms</groupId>
                <artifactId>wms-inbound</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.wms</groupId>
                <artifactId>wms-outbound</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.wms</groupId>
                <artifactId>wms-inventory</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.wms</groupId>
                <artifactId>wms-strategy</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.wms</groupId>
                <artifactId>wms-task</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.wms</groupId>
                <artifactId>wms-print</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Hutool (comprehensive utility library) -->
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool.version}</version>
            </dependency>

            <!-- Knife4j (API documentation, enhanced Swagger) -->
            <!-- 以下三项覆盖 knife4j 传递进来的 springdoc 2.3.0，保持三个 artifact 版本一致 -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-api</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-common</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>

            <!-- MapStruct (DTO &lt;-&gt; Domain object mapping) -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- EasyExcel -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>easyexcel</artifactId>
                <version>4.0.3</version>
            </dependency>

            <!--
              以下三项不在 Spring Boot BOM 的管理范围内，且 EasyExcel 4.0.3 传递进来的版本带有已知漏洞：
                poi-ooxml        5.2.5  -> 5.4.0 起修复 GHSA-gmg8-593g-7mv3（OOXML 解析输入校验）
                commons-compress 1.25.0 -> 1.26.0 起修复 GHSA-4265-ccf5-phj5 / GHSA-4g9r-vxhx-9pgx
                commons-lang3    3.14.0 -> 3.18.0 起修复 GHSA-j288-q9x7-2f5v
              这里取各修复线的最小版本（跳版幅度最小，且已用 OSV 核对无已知漏洞）。
            -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi</artifactId>
                <version>${poi.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml-lite</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- Tomcat 没有官方 BOM，三个 artifact 需逐个覆盖（见 properties 中的说明） -->
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-core</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-el</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-websocket</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-compress</artifactId>
                <version>${commons-compress.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>

            <!-- Redisson (distributed lock) -->
            <dependency>
                <groupId>org.redisson</groupId>
                <artifactId>redisson-spring-boot-starter</artifactId>
                <version>3.40.2</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- All modules inherit these -->

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.spotify.fmt</groupId>
                <artifactId>fmt-maven-plugin</artifactId>
                <configuration>
                    <style>aosp</style>
                </configuration>
                <executions>
                    <execution>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>com.spotify.fmt</groupId>
                    <artifactId>fmt-maven-plugin</artifactId>
                    <version>2.23</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
